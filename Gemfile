source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in avatarro.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end

gem 'puma'

# To use a debugger
# gem 'byebug', group: [:development, :test]
